<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.bloomberg.datalake.trino</groupId>
        <artifactId>datalake-bas-reactor-bom</artifactId>
        <version>480.1</version>
        <relativePath>../../lib/datalake-bas-reactor-bom/pom.xml</relativePath>
    </parent>

    <artifactId>datalake-bas</artifactId>
    <packaging>trino-plugin</packaging>
    <description>Trino - Datalake BAS Connector</description>

    <properties>
        <air.main.basedir>${project.parent.basedir}</air.main.basedir>
        <dep.jinja.version>2.7.3</dep.jinja.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- jinjava requires com.fasterxml.jackson.dataformat:jackson-dataformat-yaml, which transitively
             requires org.yaml:snakeyaml. Pin to the version managed by the root POM. -->
            <dependency>
                <groupId>org.yaml</groupId>
                <artifactId>snakeyaml</artifactId>
                <version>2.6</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>com.bloomberg.bas</groupId>
            <artifactId>codecfactory</artifactId>
        </dependency>

        <dependency>
            <groupId>com.bloomberg.bas-reactor</groupId>
            <artifactId>bas-reactor-client</artifactId>
        </dependency>

        <dependency>
            <groupId>com.bloomberg.bas-reactor.external.basmessage</groupId>
            <artifactId>basmessage</artifactId>
        </dependency>

        <dependency>
            <groupId>com.bloomberg.bas-reactor.external.schema</groupId>
            <artifactId>schema</artifactId>
        </dependency>

        <dependency>
            <groupId>com.bloomberg.bas-reactor.schemas</groupId>
            <artifactId>bsct</artifactId>
        </dependency>

        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
        </dependency>

        <dependency>
            <groupId>com.google.inject</groupId>
            <artifactId>guice</artifactId>
            <classifier>classes</classifier>
        </dependency>

        <dependency>
            <groupId>com.hubspot.jinjava</groupId>
            <artifactId>jinjava</artifactId>
            <version>${dep.jinja.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>com.google.code.findbugs</groupId>
                    <artifactId>annotations</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>bootstrap</artifactId>
        </dependency>

        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>configuration</artifactId>
        </dependency>

        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>json</artifactId>
        </dependency>

        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>log</artifactId>
        </dependency>

        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>units</artifactId>
        </dependency>

        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-core</artifactId>
        </dependency>

        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-plugin-toolkit</artifactId>
            <exclusions>
                <!-- not used and conflicts with bas-reactor on org.hdrhistogram:HdrHistogram -->
                <exclusion>
                    <groupId>io.airlift</groupId>
                    <artifactId>stats</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
        </dependency>

        <dependency>
            <groupId>org.reactivestreams</groupId>
            <artifactId>reactive-streams</artifactId>
        </dependency>

        <dependency>
            <groupId>org.threeten</groupId>
            <artifactId>threeten-extra</artifactId>
        </dependency>

        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>slice</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-api</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-context</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Trino SPI -->
        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-spi</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>com.bloomberg</groupId>
            <artifactId>uncheck</artifactId>
            <version>3.1.56</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>com.bloomberg.bas-reactor.external.basmessage</groupId>
            <artifactId>bytebuf</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>com.bloomberg.bas-reactor</groupId>
            <artifactId>annotations</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.bloomberg.bas-reactor</groupId>
            <artifactId>bas-reactor-server</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.bloomberg.codegen</groupId>
            <artifactId>bxsdsvc</artifactId>
            <version>1.7-jakarta-2.0.25</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.bloomberg.codegen</groupId>
            <artifactId>crsrvpx</artifactId>
            <version>1.30-jakarta-2.0.25</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.bloomberg.codegen</groupId>
            <artifactId>etsdspbisvc</artifactId>
            <version>1.0-jakarta-2.0.25-SNAPSHOT</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.bloomberg.testcontainers</groupId>
            <artifactId>bas-junit</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>configuration-testing</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>testing</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-main</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>com.teradata</groupId>
                    <artifactId>re2j-td</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>io.airlift</groupId>
                    <artifactId>jaxrs</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-testing</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>3.2.4</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-dependency-plugin</artifactId>
                <configuration>
                    <!-- com.fasterxml.jackson.core:jackson-core is used directly in test, but it is also a required compile transitive dependency;
                         the dependency analysis want it to be scoped test-only, but breaks compilation;
                         there might be a better way to fix it -->
                    <ignoredNonTestScopedDependencies>
                        <ignoredNonTestScopedDependency>com.fasterxml.jackson.core:jackson-core::</ignoredNonTestScopedDependency>
                    </ignoredNonTestScopedDependencies>
                    <ignoredUsedUndeclaredDependencies>
                        <ignoredUsedUndeclaredDependency>com.fasterxml.jackson.core:jackson-core::</ignoredUsedUndeclaredDependency>
                    </ignoredUsedUndeclaredDependencies>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
